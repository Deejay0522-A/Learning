from fasthtml.common import * 
from starlette.background import BackgroundTask

# Copy and paste code you find in here for websites
# I recommend using port 5001 and using the http://localhost:5001 address
# you can also switch the number at the end, being the port if using a different port.
#ex: http://localhost:8000 | http://localhost:9912
# ways to switch ports: serve(port={port number})